# dependencies
node_modules/

# build output
dist/
.astro/

# environment
.env
.env.production
.env.*.local

# editor / OS
.vscode/
.DS_Store

# logs
*.log
npm-debug.log*

# internal working papers — this repository is PUBLIC. These are SEO
# measurement notes (baselines, pre-registered hypotheses, deferred-decision
# logic), not site source. Kept on disk, out of the repo. Listed here so a
# `git add -A` cannot sweep them back in.
GSC-DVH-BASELINE.md
PRODUCTION-BASELINE.md
