---
// Thin master wrapper — the page is implemented once in
// src/components/content/AboutPage.astro and shared with the dynamic
// localized route (playbook §6.5: one component per page, no duplication).
import AboutPage from '../components/content/AboutPage.astro';
---

<AboutPage />
