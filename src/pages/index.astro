---
// Thin master wrapper — the homepage is implemented once in
// src/components/content/HomePage.astro, shared with the dynamic route
// (which emits /es.html when es/index.json is committed).
import HomePage from '../components/content/HomePage.astro';
---

<HomePage />
